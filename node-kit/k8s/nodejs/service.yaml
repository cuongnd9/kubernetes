apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: production
spec:
  selector:
    app: myapp
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  externalTrafficPolicy: Local
  type: LoadBalancer
